[tool.poetry]
name = "electronics-shop-project"
version = "0.1.0"
description = ""
authors = ["Vitaliy Petrenko <mail@v-petrenko.ru>"]

[tool.poetry.dependencies]
python = "^3.10"
pytest = "^7.3.1"
pytest-cov = "^4.1.0"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
